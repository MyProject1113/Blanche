package com.blanche.cc.service;

/*import java.util.List;

import org.apache.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.blanche.cc.dao.CcDao;
import com.blanche.cc.vo.CcVO;*/

/*@Service	// 구현체 생성시에는 반드시 넣어 주어야함!!!
@Transactional*/
public class CcServiceImpl {
/*public class CcServiceImpl implements CcService {
	
	Logger logger = Logger.getLogger(CcServiceImpl.class);
	
	@Autowired
	private CcDao ccDao;
	
	// 글목록 구현
	@Override
	public List<CcVO> ccList() {
		List<CcVO> myList = null;
		myList = ccDao.ccList();
		return myList;
	}
	
	// 글입력 구현
	@Override
	public int ccInsert(CcVO cvo) {
		int result = 0;
		result = ccDao.ccInsert(cvo);
		return result;
	}
	
	// 글상세 구현
	@Override
	public CcVO ccDetail(CcVO cvo) {
		CcVO detail = null;
		detail = ccDao.ccDetail(cvo);
		return detail;
	}
	
	// 비밀번호 확인 구현
	@Override
	public int pwdConfirm(CcVO cvo) {
		int result = 0;
		result = ccDao.pwdConfirm(cvo);
		return result;
	}
	
	// 글수정 구현
	@Override
	public int ccUpdate(CcVO cvo) {
		int result = 0;
		result = ccDao.ccUpdate(cvo);
		return result;
	}
	
	// 글삭제 구현
	@Override
	public int ccDelete(int b_num) {
		int result = 0;
		result = ccDao.ccDelete(b_num);
		return result;
	}
*/
}
